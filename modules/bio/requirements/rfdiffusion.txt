# RFdiffusion — Python 3.10, torch 1.12.1 + cu113 (Ampere sm_86 supported).
# NOTE: numpy MUST stay <2 for torch 1.12. dgl, dllogger, SE3Transformer and the
# rfdiffusion package itself are installed imperatively by bio-setup (find-links /
# git / local paths that don't belong in a plain requirements file).
--extra-index-url https://download.pytorch.org/whl/cu113
torch==1.12.1+cu113
torchaudio==0.12.1+cu113
torchvision==0.13.1+cu113
numpy==1.26.4
hydra-core==1.3.2
omegaconf==2.3.0
pyrsistent==0.20.0
e3nn==0.3.3
wandb==0.12.0
pynvml==11.0.0
decorator==5.1.0
scipy==1.13.1
opt_einsum==3.3.0
opt-einsum-fx==0.1.4
icecream==2.1.3
